# Auto detect text files and normalize line endings to LF
* text=auto eol=lf

# Windows scripts keep CRLF
*.{cmd,bat,ps1} text eol=crlf

# Binary files
*.{png,jpg,jpeg,gif,webp,ico,svg,pdf,woff,woff2,ttf,eot} binary
