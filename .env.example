# 서버 전용 API 키 — 실제 값은 .env.local(로컬) / Vercel 환경변수(배포)에만 둔다.
# 클라이언트(NEXT_PUBLIC_*)로 절대 노출 금지.

GEMINI_API_KEY=
TYPECAST_API_KEY=
TYPECAST_DEFAULT_VOICE_ID=

# 선택: Gemini 모델 오버라이드 (미설정 시 코드 기본값 사용)
# GEMINI_MODEL=
